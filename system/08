# What file contains the RSA keys of systems that are authorized to connect via SSH?
# Answer: ~/.ssh/authorized_keys
# Solution: you need to create the .ssh directory and the authorized_keys file for the newly-created VM with mkdir, touch and chmod.